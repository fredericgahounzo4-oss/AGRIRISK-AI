Django>=5.0,<6.2
djangorestframework>=3.15
django-cors-headers>=4.4
Pillow>=10.4
requests>=2.32
reportlab>=4.2
python-dotenv>=1.0

# Déploiement (Render + Neon)
dj-database-url>=2.2
psycopg[binary]>=3.2
gunicorn>=22.0
whitenoise>=6.7

# Stockage des fichiers médias (avatars, photos de diagnostic) sur Cloudinary
# — indispensable en production car le disque de Render est éphémère.
django-cloudinary-storage>=0.3
cloudinary>=1.41
